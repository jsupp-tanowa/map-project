version: 0.9.9.9.9
release_date: 2026-08-01
summary: フッターエリアの修正⑤

changes:
  - .tab-bar の bottom を、複雑な env()/calc() の式から固定値 -15px に変更(高さ53pxはそのまま)
#map の高さ計算式も、フッターが15px下がった分だけ拡張(165px → 150px)
.bottom-sheet(店舗カード)の位置も、タブバーとの間隔(10px)を保つよう 48px に調整

notes:
  - 本番環境（Firebase Hosting）へのデプロイ
  - 今後の機能追加は dev ブランチで行い、main に merge して反映
