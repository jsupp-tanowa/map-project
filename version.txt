version: 1.2.5
release_date: 2026-09-05
summary: ズームボタン修正

changes:
  - #mapはposition: relativeではないため、.zoom-control-areaの絶対配置はページ全体（body）基準になり、ヘッダー直下に表示されていました。

#mapのmargin-top（ヘッダー＋検索バー分の112px + env(safe-area-inset-top)）と同じ値をtopに加算し、そこからさらに10px内側に入れることで、見た目上は地図の右上角に来るよう修正しました。.button-area（現在地・再読み込みボタン）はbottom基準のため元々この問題は起きていません。

notes:
  - 本番環境（Firebase Hosting）へのデプロイ
  - 今後の機能追加は dev ブランチで行い、main に merge して反映
